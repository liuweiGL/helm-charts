{{- /*
Copyright VMware, Inc.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if .Values.dashboard.ingress.enabled }}
apiVersion: {{ include "common.capabilities.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ include "rocketmq.dashboard.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
  {{- if or .Values.dashboard.ingress.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.dashboard.ingress.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.dashboard.ingress.ingressClassName (eq "true" (include "common.ingress.supportsIngressClassname" .)) }}
  ingressClassName: {{ .Values.dashboard.ingress.ingressClassName | quote }}
  {{- end }}
  rules:
    {{- if .Values.dashboard.ingress.hostname }}
    - host: {{ .Values.dashboard.ingress.hostname | quote }}
      http:
        paths:
          {{- if .Values.dashboard.ingress.extraPaths }}
          {{- toYaml .Values.dashboard.ingress.extraPaths | nindent 10 }}
          {{- end }}
          - path: {{ .Values.dashboard.ingress.path }}
            {{- if eq "true" (include "common.ingress.supportsPathType" .) }}
            pathType: {{ .Values.dashboard.ingress.pathType }}
            {{- end }}
            backend: {{- include "common.ingress.backend" (dict "serviceName" (include "rocketmq.dashboard.fullname" .) "servicePort" "dashboard")  | nindent 14 }}
    {{- else if .Values.dashboard.ingress.path }}
    - http:
        paths:
          {{- if .Values.dashboard.ingress.extraPaths }}
          {{- toYaml .Values.dashboard.ingress.extraPaths | nindent 10 }}
          {{- end }}
          - path: {{ .Values.dashboard.ingress.path }}
            {{- if eq "true" (include "common.ingress.supportsPathType" .) }}
            pathType: {{ .Values.dashboard.ingress.pathType }}
            {{- end }}
            backend: {{- include "common.ingress.backend" (dict "serviceName" (include "rocketmq.dashboard.fullname" .) "servicePort" "dashboard")  | nindent 14 }}
    {{- end }}
    {{- range .Values.dashboard.ingress.extraHosts }}
    - host: {{ .name | quote }}
      http:
        paths:
          - path: {{ default "/" .path }}
            {{- if eq "true" (include "common.ingress.supportsPathType" $) }}
            pathType: {{ default "ImplementationSpecific" .pathType }}
            {{- end }}
            backend: {{- include "common.ingress.backend" (dict "serviceName" (include "rocketmq.dashboard.fullname" $) "servicePort" "dashboard") | nindent 14 }}
    {{- end }}
    {{- if .Values.dashboard.ingress.extraRules }}
    {{- include "common.tplvalues.render" (dict "value" .Values.dashboard.ingress.extraRules "context" $) | nindent 4 }}
    {{- end }}
  {{- if or (and .Values.dashboard.ingress.tls (or (include "common.ingress.certManagerRequest" ( dict "annotations" .Values.dashboard.ingress.annotations )) .Values.dashboard.ingress.selfSigned (not (empty .Values.dashboard.ingress.secrets)))) .Values.dashboard.ingress.extraTls }}
  tls:
    {{- if and .Values.dashboard.ingress.tls (or (include "common.ingress.certManagerRequest" ( dict "annotations" .Values.dashboard.ingress.annotations )) .Values.dashboard.ingress.selfSigned (not (empty .Values.dashboard.ingress.secrets))) }}
    - hosts:
        - {{ .Values.dashboard.ingress.hostname | quote }}
        {{- if or (.Values.dashboard.ingress.tlsWwwPrefix) (eq (index .Values.dashboard.ingress.annotations "nginx.ingress.kubernetes.io/from-to-www-redirect") "true" ) }}
        - {{ printf "www.%s" (tpl .Values.dashboard.ingress.hostname $) | quote }}
        {{- end }}
      secretName: {{ printf "%s-tls" .Values.dashboard.ingress.hostname }}
    {{- end }}
    {{- if .Values.dashboard.ingress.extraTls }}
    {{- include "common.tplvalues.render" (dict "value" .Values.dashboard.ingress.extraTls "context" $) | nindent 4 }}
    {{- end }}
  {{- end }}
{{- end }}
